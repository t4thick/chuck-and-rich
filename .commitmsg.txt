Security hardening + full audit cleanup

Security & infra (local additions):
- Admin login: in-memory rate limiter (5 attempts / 15 min) with proper 429 + Retry-After
- Admin uploads: validate actual file bytes via magic-byte sniffing (defeats polyglot uploads)
- Admin order notes: cap at 500 chars to prevent oversized payloads
- Admin session TTL: 7 days -> 8 hours
- next.config: add CSP, HSTS, and tighter headers on /admin/*
- Store: app/(store)/error.tsx error boundary for graceful Supabase failures

SEO & performance (local additions):
- Product page: OpenGraph + Twitter meta + JSON-LD Product structured data
- ProductCard: forward `priority` so first-row LCP images preload
- ProductShowcase: new `priorityCount` prop wired through to homepage trending row
- Supabase queries: replace select('*') with explicit columns (smaller payloads)
- Homepage: fetch 16 products and split into non-overlapping trending/new-arrivals
  (was showing the same products reversed)

UX (local additions):
- CartDrawer: iOS-safe scroll lock (position:fixed), focus trap, Esc-to-close,
  44x44 quantity controls per AGENTS.md
- MobileBottomNav: bump label size 10px -> 11px for readability

Audit cleanup (this commit):
- Drop dead exports from lib/constants/collections.ts
  (COOK_TONIGHT_BUNDLES, SHOP_BY_COUNTRY, TESTIMONIALS, RECIPE_INSPO had no consumers)
- Strip `font-display` from chrome surfaces (checkout, account, order-confirmation,
  track-order, AuthShell, AdminSidebar) -> sans + font-semibold per AGENTS.md
- Drop Cormorant_Garamond font + --font-display CSS var entirely (no consumers left)
- Rename `.asafo-category-tile` -> `.category-tile` (drop legacy brand prefix)
- ProductImage hover: 500ms -> 200ms transition; normalize to bg-earth-50
- Refresh Playwright homepage/navbar specs to match current copy & a11y roles

Verified: production build succeeds, 27 routes, zero TS/lint errors.
